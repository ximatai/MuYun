pluginManagement {
    repositories {
        mavenCentral()
        gradlePluginPortal()
        mavenLocal()
    }
    plugins {
        id "${quarkusPluginId}" version "${quarkusPluginVersion}"
    }
}

rootProject.name = 'muyun'

include 'my-core'
//include 'my-core-uni'
include 'my-platform'
//include 'my-msg'
include 'my-boot'
